function initGameHandlers(io, socket) {
  // Game-specific event handlers go here.
  // All standard game-flow events (host:*, player:*, timer:*)
  // are registered by their respective handler modules.
}

module.exports = initGameHandlers
